{
  "version": 2,
  "buildCommand": "yarn vercel-build",
  "outputDirectory": "dist",
  "installCommand": "yarn install",
  "framework": null,
  "ignoreCommand": "if ! git rev-parse --verify HEAD^ >/dev/null 2>&1; then exit 1; fi; ! (git diff --name-only HEAD^ HEAD | grep -qE '^backend/' || git diff --name-only HEAD^ HEAD | grep -qE '^(package\\.json|yarn\\.lock)$')",
  "functions": {
    "api/index.ts": {
      "memory": 1024,
      "maxDuration": 30
    }
  },
  "rewrites": [
    {
      "source": "/(.*)",
      "destination": "/api/index"
    }
  ]
}
